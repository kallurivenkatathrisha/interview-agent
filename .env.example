# Copy this file to .env and fill in your key, or export it in your shell.
# Optional: if unset, the agent automatically falls back to a deterministic
# heuristic grader so the app still runs end-to-end with zero setup.
ANTHROPIC_API_KEY=sk-ant-...
